# Cursor Rules
# 强制遵守 SDD（规范驱动开发）与 AGENTS.md 工作流

1. 每次修改业务逻辑前，必须首先阅读并更新 SPEC.md，将用户需求固化为文档契约。
2. 修复 Bug 或添加新功能时，必须首先编写针对性的测试用例，防止历史功能退化。
3. 修改代码后必须运行项目测试套件并保证 100% 通过。
4. 受到用户批评或纠错时，主动将教训写入 AGENTS.md 底部的避坑清单中。
5. 详细行为准则见根目录下 AGENTS.md。
